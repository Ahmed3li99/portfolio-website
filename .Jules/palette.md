## 2025-02-16 - Custom Cursor Keyboard Accessibility
**Learning:** When using a custom cursor via CSS `cursor: none`, native browser focus indicators are less visible or completely hidden. This creates a severe accessibility barrier for keyboard users.
**Action:** Always ensure high-contrast explicit `:focus-visible` styles are implemented on interactive elements when a custom cursor is used in a project.
